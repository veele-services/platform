# Fieldflow Calm protected-root canary

This non-normative file exists only to trigger a disposable pull request against
`codex/fieldgrid-uiux-master`. The pull request verifies that the trusted
default-branch workflow can read the protected contract root and publish the
required `Fieldflow Calm contract root` check for the exact candidate commit.

The canary must be closed without merging. It does not advance lifecycle state,
change runtime behavior, access staging data, or authorize deployment.
